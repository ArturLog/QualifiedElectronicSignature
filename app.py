from classes.AppController import AppController

if __name__ == "__main__":
    # Create an instance of the AppController class.
    app = AppController()

    # Enter the main event loop of the application.
    # This starts the GUI and keeps the application running until it is closed.
    app.mainloop()
